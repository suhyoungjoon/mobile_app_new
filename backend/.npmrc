# npm 설정 최적화
# Puppeteer는 PDF 생성 및 화면 캡처에 필요하므로 정상 설치

# 네트워크 최적화
fetch-retries=3
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000

# 설치 최적화
prefer-offline=false
progress=true

# postinstall 스크립트 오류 무시 (일부 패키지의 postinstall이 실패해도 계속 진행)
ignore-scripts=false

# Puppeteer postinstall 스크립트 건너뛰기 (빌드 시 Chromium 다운로드 방지)
# 런타임에 필요할 때 자동으로 다운로드됨

# Puppeteer 빌드 최적화: 빌드 시 Chromium 다운로드 건너뛰기 (런타임에 필요시 다운로드)
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true

